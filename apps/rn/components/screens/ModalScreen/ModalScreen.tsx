import { View } from "react-native";

import { WuiText } from "wui-rn/src";

const ModalScreen = (): React.ReactNode => {
  return (
    <View>
      <WuiText>ModalScreen</WuiText>
    </View>
  );
};

export default ModalScreen;
