# Copy this file on the server to /www/next/projects/.env.production
# and fill in your real values. Do not commit .env.production (it is gitignored).
DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=
