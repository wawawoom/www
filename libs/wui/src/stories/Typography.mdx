import { Canvas, Meta } from "@storybook/blocks";

import * as TypographyStories from "./Typography.stories";

<Meta of={TypographyStories} />

# Typography

Full document example using **WuiTitle** and **WuiText** with different sizes and heading levels (H1 to H6).

<Canvas of={TypographyStories.DocumentExample} sourceState="none" />
