# FTP deployment (required for pnpm run deploy)
# Prefer using .env at the workspace root (see / .env.example).
# Or copy this file to libs/wui/.env and fill in your values. Do not commit .env.

FTP_PASS="your_ftp_password"

# Optional overrides (defaults are set in deploy-wui.sh):
# FTP_HOST="ftp.cluster015.hosting.ovh.net"
# FTP_USER="wawawoom"
# FTP_PORT="21"
